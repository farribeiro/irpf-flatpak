app-id: br.gov.rfb.irpf
branch: stable
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk-extensions: org.freedesktop.Sdk.Extension.openjdk11
sdk: org.freedesktop.Sdk
command: /app/irpf.sh
modules:
  - name: irpf
    buildsystem: simple
    build-commands:
      - /usr/lib/sdk/openjdk11/install.sh
      - sh install.sh
      - install -D br.gov.rfb.irpf.desktop /app/share/applications/br.gov.rfb.irpf.desktop
    sources:
      - type: archive
        url: https://downloadirpf.receita.fazenda.gov.br/irpf/2021/irpf/arquivos/IRPF2021-1.9.zip
        sha256: 1d0cdbb00e141caf2d65b85be88b560b86b3633bf117b7d34221659b89c8e5ee
      - type: file
        path: irpf.sh
      - type: file
        path: install.sh
      - type: file
        path: br.gov.rfb.irpf.desktop
finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=x11
  # Wayland access
  - --socket=wayland
  # Needs to talk to the network:
  - --share=network
  # Needs to save files locally
  - --filesystem=xdg-documents
  - --filesystem=home
  - --env=PATH=$PATH:/app/jre/bin:/usr/bin
# cleanup:
#   - '/IRPF2021'
